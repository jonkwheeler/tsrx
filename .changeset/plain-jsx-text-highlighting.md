---
"@ripple-ts/vscode-plugin": patch
---

Use the regular editor text color for plain JSX text and inline `<code>` contents in TSRX files, without embedded JSX tag scopes leaking into text children.
