{
  "name": "@benchmarks/recursive-context",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Recursive-component + Context-read bench. Same DOM shape rendered by ripple, react, and solid; harness measures mount / update / unmount independently.",
  "scripts": {
    "bench": "node run.mjs",
    "bench:long": "node run.mjs 40"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
