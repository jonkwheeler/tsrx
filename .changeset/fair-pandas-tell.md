---
'@tsrx/bun-plugin-react': patch
---

Add a Bun plugin for compiling `.tsrx` files with `@tsrx/react`, handling the
final React JSX transform with Bun's TSX pipeline, and emitting component-local
styles as virtual CSS modules.
