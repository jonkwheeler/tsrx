---
'@ripple-ts/vite-plugin': patch
'@ripple-ts/rollup-plugin': patch
'@ripple-ts/typescript-plugin': patch
'@ripple-ts/language-server': patch
'@ripple-ts/prettier-plugin': patch
'@ripple-ts/eslint-plugin': patch
---

Add first-phase `.tsrx` support across the core Ripple tooling so Vite, Rollup, TypeScript, the language server, Prettier, ESLint, and editor integrations accept both `.ripple` and `.tsrx` files.
