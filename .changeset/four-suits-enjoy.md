---
'@tsrx/bun-plugin-vue': patch
---

Add a Bun plugin for compiling `.tsrx` files with `@tsrx/vue`, running the
downstream `vue-jsx-vapor` transform, and emitting component-local styles as
virtual CSS modules.
